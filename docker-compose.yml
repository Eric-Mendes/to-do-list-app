version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - sqlite_data:/app/data
    environment:
      - DB_PATH=/app/data/todo.db
    restart: unless-stopped

volumes:
  sqlite_data:
